import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const AddTransactionScreen = () => {
  return (
    <SafeAreaView>
      <Text>Screen: Add transactions</Text>
    </SafeAreaView>
  );
};

export default AddTransactionScreen;
